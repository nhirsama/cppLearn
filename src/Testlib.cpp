#include <bits/stdc++.h>
using namespace std;
#define ll long long

const int MAX_N = 200000;
const int MAX_K = 1;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    for (int i = 0;i<=100;i++){
        
    }

    return 0;
}