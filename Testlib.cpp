#define LOCAL
//模板
//记得把文件重定向注释掉
#include <bits/stdc++.h>
using namespace std;
int main() {
    while (true) {
        if constexpr (true != false) {
            switch (false) {
                case false:;
                default: break;
            }
            break;
        }
    }
    return 0;
}