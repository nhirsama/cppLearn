#include <bits/stdc++.h>

using std::cin;
using std::cout;
using std::vector;
using std::multimap;

using ll = long long;

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m, k;
    cin >> n >> m >> k;
    vector<multimap<ll, ll>::iterator> index(n + 1);
    multimap<ll, ll> tree;
    for (int i = 1, t, x, y; i <= m; ++i) {
        cin >> t >> x >> y;

    }
}
