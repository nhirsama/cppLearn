#define LOCAL
//P1024 [NOIP2001 提高组] 一元三次方程求解
//记得把文件重定向注释掉
#include <bits/stdc++.h>
using namespace std;
int main() {
#ifdef LOCAL
    freopen("Testlib.in", "r", stdin);
    freopen("Code.out", "w", stdout);
#endif
    double a,b,c,d;
    cin>>a>>b>>c>>d;


#ifdef LOCAL
    fclose(stdin);
    fclose(stdout);
#endif
    return 0;
}