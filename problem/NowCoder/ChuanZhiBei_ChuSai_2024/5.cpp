#include <algorithm>
#include <iostream>
using namespace std;
int n;
int arr[2005];
int dfs(int m) {
    int l = m, r = m;
    int ans = 0;
    for (int i = 1;i<n;i++) {
        if (arr)
    }
}
int main() {
    cin>>n;
    int minNum=INT_MAX;
    for(int i=1;i<=n;i++) {
        scanf("%d",&arr[i]);
        if(arr[i]<arr[minNum]) minNum=i;
    }

}
// 64 位输出请用 printf("%lld")