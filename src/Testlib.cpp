#include <bits/stdc++.h>
#define int long long
using namespace std;
using i64 = long long;
using i32 = long long;

void solve() {

}

int main() {
    ios::sync_with_stdio(fasle);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}