//P11996 我是黄色恐龙大将军
#include <bits/stdc++.h>
using i128 = __int128;
using i64 = long long int;
using i32 = int;
typedef std::pair<i32, i32> pii;
constexpr int N = 1e5 + 10;
constexpr int mod = 1e9 + 7;
#define IOS std::ios::sync_with_stdio(false); std::cin.tie(nullptr); std::cout.tie(nullptr);
#define x first
#define y second
#define endl '\n'
#define all(x) (x).begin(), (x).end()

void nhir() {
    // std::cout<<9<<endl;
    // std::set<i32> s;
    // i64 a = 1, b = 1;
    // for (i32 i = 1; i <= 15; i++) {
    //     a *= 2, b *= 5;
    //     std::cout << a<< ' '<<b << endl;
    // }
    std::cout<<5+6+7+8+9+10<<endl;
}

signed main() {
    IOS;
    i32 T = 1;
    while (T--) nhir();
    return 0;
}
