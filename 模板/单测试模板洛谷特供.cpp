//模板
#include <bits/stdc++.h>
using namespace std;

int main() {
    if (getenv("ONLINE_JUDGE") == nullptr) {
        freopen("Testlib.in", "r", stdin);
        freopen("Code.out", "w", stdout);
    }

    return 0;
}
