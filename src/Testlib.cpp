#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
class cla{
private:
    string name ;
};
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cla::name ="adf";

    return 0;
}
